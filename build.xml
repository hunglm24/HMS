<?xml version="1.0" encoding="UTF-8"?>
<project name="WebApplication1" default="default" basedir=".">
    <description>Builds, tests, and runs the project WebApplication1.</description>
    <import file="nbproject/build-impl.xml"/>

    <!--
    The following targets are empty hooks that NetBeans can override.
    Keep them here so custom Ant targets can be added later without
    breaking the generated build flow.
    -->
    <target name="-pre-init"/>
    <target name="-post-init"/>
    <target name="-pre-compile"/>
    <target name="-post-compile"/>
    <target name="-pre-clean"/>
    <target name="-post-clean"/>
</project>
